export interface UserData {
  height: number | null;
  weight: number | null;
  beers: number;
  wines: number;
  cocktails: number;
}

export interface TestResult {
  passed: boolean;
  retryAvailable: boolean;
  waitTime?: string;
}

export interface TestResult {
  passed: boolean;
  retryAvailable: boolean;
  waitTime?: string;
}
